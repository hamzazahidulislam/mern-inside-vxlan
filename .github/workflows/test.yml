# @format

name: Test Github Actions
on:
  push:
    branches:
      - main

jobs:
  print-hello-world:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ssh -i ./key.pem ubuntu@$15.206.73.72 "echo Hello World 1"
